---
const { enabled = false } = Astro.props;
---
{enabled && (
  <script>
    (async () => {
      // 动态导入 vConsole
      const { default: VConsole } = await import('vconsole');
      new VConsole({});
    })();
  </script>
)}